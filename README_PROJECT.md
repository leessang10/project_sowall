# Project SoWall

AVEC 스타일의 인테리어 디자인 & 브랜딩 웹사이트

## 🚀 시작하기

### 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000) 접속

### 빌드

```bash
npm run build
npm start
```

## 🎨 디자인 특징

- **폰트**: Outfit (본문), Forum (제목)
- **색상**: 흰색 배경 (#FFFFFF), 검정 텍스트 (#191919)
- **애니메이션**: GSAP ScrollTrigger 기반
- **레이아웃**: Flexbox & Grid, 완전 반응형
- **스타일**: Tailwind CSS

## 📁 프로젝트 구조

```
project_sowall/
├── app/
│   ├── layout.tsx          # 루트 레이아웃, 폰트 설정
│   ├── page.tsx             # 메인 페이지
│   └── globals.css          # 전역 스타일
├── components/
│   ├── Header.tsx           # 헤더 (네비게이션)
│   ├── Hero.tsx             # 히어로 섹션
│   ├── Gallery.tsx          # 인테리어 갤러리 (드래그 스크롤)
│   ├── Branding.tsx         # 브랜딩 섹션
│   ├── Consulting.tsx       # 컨설팅 섹션
│   ├── Clients.tsx          # 클라이언트 섹션
│   ├── Contact.tsx          # 컨택트 폼
│   └── Footer.tsx           # 푸터
└── public/                  # 정적 파일
```

## 🎯 주요 기능

### 1. 헤더
- 고정 네비게이션
- INTERIOR 메뉴 드롭다운
- 모바일 햄버거 메뉴
- 호버 언더라인 애니메이션

### 2. 히어로 섹션
- GSAP 페이드인 애니메이션
- 스크롤 인디케이터

### 3. 갤러리
- 마우스 드래그 스크롤
- 이미지 호버 확대 효과
- 스크롤 트리거 애니메이션

### 4. 브랜딩
- 3개의 서비스 항목
- SVG 아이콘
- 호버 시 위로 이동 효과

### 5. 컨설팅
- 3개의 컨설팅 서비스
- 스크롤 애니메이션

### 6. 클라이언트
- 반응형 그리드 레이아웃
- 호버 효과

### 7. 컨택트
- 연락처 정보
- 입력 폼 (이름, 이메일, 제목, 메시지)
- 포커스 애니메이션

## 🛠️ 기술 스택

- **Framework**: Next.js 16 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **Animation**: GSAP, ScrollTrigger
- **Fonts**: Google Fonts (Outfit, Forum)
- **Images**: Next.js Image 최적화

## 📱 반응형 디자인

- **Desktop**: 1400px max-width
- **Tablet**: 1024px 이하
- **Mobile**: 767px 이하

모바일에서는 햄버거 메뉴, 작은 폰트 크기, 축소된 spacer를 사용합니다.

## 🎨 커스터마이징

### 색상 변경
`app/globals.css`의 CSS 변수 수정:
```css
:root {
  --background: #ffffff;
  --foreground: #191919;
  --text-light: rgba(25, 27, 29, 0.6);
}
```

### 갤러리 이미지 변경
`components/Gallery.tsx`의 `galleryItems` 배열 수정

### 클라이언트 목록 변경
`components/Clients.tsx`의 `clients` 배열 수정

## 📝 라이선스

© 2026 Project SoWall. All rights reserved.
